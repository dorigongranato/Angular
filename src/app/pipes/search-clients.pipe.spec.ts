import { SearchClientsPipe } from './search-clients.pipe';

describe('SearchClientsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchClientsPipe();
    expect(pipe).toBeTruthy();
  });
});
